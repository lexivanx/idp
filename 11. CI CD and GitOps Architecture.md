## 11. CI/CD & GitOps Architecture

### 11.1 Pipeline Architecture

```
Developer Push → Azure DevOps Pipeline → Build & Test → Container Image → ACR
                                                                           │
                                                                           ▼
                                                              GitOps Repo Update
                                                                           │
                                                                           ▼
                                                              ArgoCD Sync (auto/manual)
                                                                           │
                                                              ┌────────────┼────────────┐
                                                              ▼            ▼             ▼
                                                            Dev        Staging      Production
                                                          (auto)     (auto+gate)   (manual+approval)
```

### 11.2 GitOps Repository Structure

```
gitops/
├── apps/                              # ArgoCD Application definitions
│   ├── platform/
│   │   ├── catalog-service.yaml
│   │   ├── provisioner-service.yaml
│   │   └── ...
│   └── workloads/
│       └── team-payments/
│           └── payment-api.yaml
│
├── environments/
│   ├── dev/
│   │   ├── platform/
│   │   │   ├── catalog-service/
│   │   │   │   └── values.yaml       # Dev-specific Helm overrides
│   │   │   └── ...
│   │   └── workloads/
│   ├── staging/
│   │   ├── platform/
│   │   └── workloads/
│   └── production/
│       ├── platform/
│       └── workloads/
│
├── base/                              # Base Helm charts
│   ├── platform/
│   │   ├── catalog-service/
│   │   │   ├── Chart.yaml
│   │   │   ├── values.yaml
│   │   │   └── templates/
│   │   │       ├── deployment.yaml
│   │   │       ├── service.yaml
│   │   │       ├── hpa.yaml
│   │   │       ├── pdb.yaml
│   │   │       ├── networkpolicy.yaml
│   │   │       ├── servicemonitor.yaml
│   │   │       └── configmap.yaml
│   │   └── ...
│   └── common/
│       └── standard-service/          # Reusable base chart
│
└── cluster-config/
    ├── namespaces/
    ├── rbac/
    ├── network-policies/
    ├── resource-quotas/
    └── istio/
        ├── gateway.yaml
        ├── peer-authentication.yaml
        └── authorization-policies/
```

### 11.3 ArgoCD Configuration

```yaml
# ArgoCD Application for catalog-service
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: catalog-service
  namespace: argocd
  annotations:
    notifications.argoproj.io/subscribe.on-sync-succeeded.slack: platform-deployments
    notifications.argoproj.io/subscribe.on-sync-failed.slack: platform-alerts
spec:
  project: cloudmultiploy-platform
  source:
    repoURL: https://github.com/cloudmultiploy/gitops.git
    targetRevision: HEAD
    path: environments/production/platform/catalog-service
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: cloudmultiploy-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
      - PruneLast=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        maxDuration: 3m
        factor: 2
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas  # Managed by HPA
```